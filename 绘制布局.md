# 当前绘制布局

以下坐标均为 OpenCV `cv2.putText(img, text, (x, y), ...)` 的左下角基准点。

## 常驻 HUD: ellseg_scorer.py

| 区域 | 坐标 | 内容 | 来源 |
|---|---:|---|---|
| 左上状态 | `(10, 22)` | `PASS` / `FAIL` | 眼球总通过状态 |
| 右上 FPS | `(img_w - 170, 22)` | `Cap FPS` | 采集帧率 |
| 右侧信息栏 | `x=max(340,img_w-430)` | 固定右侧栏 | 1920 宽时 x=1490 |
| 眼球偏移 | `(x, 44)` | `L:dX dY d` | 左眼原始 dx/dy/d |
| 眼球偏移 | `(x, 62)` | `R:dX dY d` | 右眼原始 dx/dy/d |
| 眼皮 | `(x, 88)` | `L_EAR` | 左眼皮 EAR |
| 眼皮 | `(x, 104)` | `R_EAR` | 右眼皮 EAR |
| 眼皮 | `(x, 120)` | `E_SYM` | 眼皮左右当前 EAR 对称 |
| 眉毛 | `(x, 146)` | `L_BIG` | 左眉 BIG 范围 |
| 眉毛 | `(x, 162)` | `R_BIG` | 右眉 BIG 范围 |
| 眉毛 | `(x, 178)` | `B_SYM` | 眉毛左右 BIG 对称 |
| 眉毛 | `(x, 194)` | `S L/R OK/BAD` | 眉毛斜率 |
| 嘴部 | `(x, 232)` | `MAR` | 嘴巴高宽比 |
| 下唇 | `(x, 250)` | `LLR` | 下唇比例 |
| 上唇 | `(x, 268)` | `ULR` | 上唇比例 |
| 嘴角 | `(x, 296)` | `MouthCorner:` | 嘴角标题 |
| 嘴角 | `(x, 312)` | `L:dX dY d` | 左嘴角偏移 |
| 嘴角 | `(x, 328)` | `R:dX dY d` | 右嘴角偏移 |
| 头部 | `(x, 354)` | `HeadPosition:` | 头部标题 |
| 头部 | `(x, 370)` | `Nose dX/dY` | 鼻尖偏移 |
| 头部 | `(x, 386)` | `Tilt` | 头部倾斜 |
| 头部 | `(x, 402)` | `Sym` | 头部对称 |
| 人中 | `(x, 412)` | `mp0:(x,y)` | 人中坐标 |
| 眼线 | `(x, 430)` | `EyeLine dY` | 按 Y 开关显示 |
| 上唇侧面 | `(x, 448)` | `ULSide tol` | 侧面上唇前点 X/Y 像素容差 |
| 上唇侧面 | `(x, 464)` | `ULSide cam/roi` | 侧面摄像头编号与固定嘴部 ROI |
| 下唇侧面 | `(x, 482)` | `LLSide tol` | 侧面下唇前点 X/Y 像素容差 |
| 下唇侧面 | `(x, 498)` | `LLSide cam/roi` | 侧面摄像头编号与固定嘴部 ROI |
| 正面摄像头 | `(x, 516)` | `FrontCam HFlip/VFlip` | 正面摄像头水平/垂直翻转配置 |
| 右下耗时 | `(img_w - 320, img_h - 8)` | `T / MP / Ell` | 检测耗时 |
| 左下开关 | `(10, img_h - 8)` | `MP / EllSeg / Landmarks` | 检测开关状态 |

## 自动眼皮调整临时 HUD: eye_auto_tuner.py

基础眼皮参数 `L_EAR/R_EAR/E_SYM` 已由常驻 HUD 显示，自动调整只追加动作信息。

| 阶段 | 坐标 | 内容 |
|---|---:|---|
| 采样/阶段 | `(10, 100)` | `[Eyelid P1/P2 Iter ...]` |
| 当前舵机 | `(10, 118)` | `A8=... A9=...` |
| P1 动作 | `(10, 118)` | `P1→L/R开/合 Δ=...` |
| P1 极限提示 | `(10, 136)` | `未通过侧到极限` |
| P2 回退 | `(10, 118)` | `P2单侧离区间→回P1` |
| P2 动作 | `(10, 118)` | `P2→合L/R 靠低EAR sym=...` |
| P2 极限提示 | `(10, 136)` | `高EAR侧闭合到极限` |
| 等待 | `(10, 118)` | `Waiting...` |
| 通过 | `(100, 80)` | `★★ EYELID PASSED ★★` |

## 自动眉毛调整临时 HUD: eye_auto_tuner.py

眉毛基础参数 `L_BIG/R_BIG/B_SYM/Slope` 已由常驻 HUD 显示。

| 阶段 | 坐标 | 内容 |
|---|---:|---|
| 采样/阶段 | `(10, 100)` | `[Eyebrow P1/P2 Iter ...]` |
| 等待 | `(10, 118)` | `Waiting...` |
| 通过 | `(100, 80)` | `EYEBROW PASSED` |

## 当前需要注意的重叠点

1. 常驻参数已集中到右侧信息栏，眉毛和眼皮不再左右挤压。
2. 自动调整临时 HUD 使用 `(10,100)/(10,118)/(10,136)`，不会覆盖右侧常驻参数。
3. 右侧信息栏高度到 `y=516`，底部耗时和左下开关不受影响。
