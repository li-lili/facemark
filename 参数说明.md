# 参数说明

## 眼球位置判断

眼球 `dx/dy/d` 现在直接使用当前虹膜中心与保存基线虹膜中心的绝对像素差：

```text
dx = 当前虹膜x - 基线虹膜x
dy = 当前虹膜y - 基线虹膜y
d  = sqrt(dx*dx + dy*dy)
```

不再用眼线整体垂直偏移 `dy_eye_line` 补偿左右眼的 `dy`。
`EyeLine HUD` 仍然可以显示整体眼线偏移，但它只作为观察信息，不参与眼球合格判断。

## 眼皮开合与对称度判断

眼皮使用 MediaPipe 关键点计算 EAR（Eye Aspect Ratio，高宽比）：

```text
EAR = mean(上下眼皮关键点距离) / 眼角宽度
```

左眼使用 `(159,145)`、`(158,153)` 两组上下眼皮点，眼角宽度使用 `(33,133)`。
右眼使用 `(386,374)`、`(385,380)` 两组上下眼皮点，眼角宽度使用 `(362,263)`。

按 `E` 保存眼皮基线后，`face_point.json` 的当前模板 `templates.<template_name>.eyelid` 保存：

```json
{
  "left_ear": 当前左眼 EAR,
  "right_ear": 当前右眼 EAR
}
```

单眼开合偏差：

```text
left_delta  = 当前左眼 EAR - 基线 left_ear
right_delta = 当前右眼 EAR - 基线 right_ear
```

眼皮对称度使用左右当前 EAR 高宽比差值：

```text
E_SYM = abs(L_EAR - R_EAR)
```

单眼是否回到基线由 `left_delta/right_delta` 判断；左右看起来是否对称由 `E_SYM` 判断。

画面显示：

```text
L_EAR=当前左眼EAR (bl=左眼基线EAR)
R_EAR=当前右眼EAR (bl=右眼基线EAR)
E_SYM=左右当前EAR差值 <= 容差
```

眼皮合格条件：

```text
abs(left_delta)  <= EYELID_EAR_TOLERANCE
abs(right_delta) <= EYELID_EAR_TOLERANCE
E_SYM            <= EYELID_EAR_TOLERANCE
```

当前：

```python
EYELID_EAR_TOLERANCE = 0.003
```

自动调整眼皮时：

```text
P1: 左右各自调各自的。left_delta/right_delta 超出容差时，该侧单独开或合，直到 L_EAR 和 R_EAR 都进入各自基线通过区间。
P2: 计算 E_SYM。若左右仍不对称，只合上当前 EAR 更高的一侧，让高宽比高的眼皮向高宽比低的一侧靠。
```

## 眉毛中性判断

当前眉毛只用你录制的自定义基线判断，不再内置 male/female 默认模板。

判断参数只有两类：

- `BIG` / `Brow-Iris Gap`：眉毛到虹膜中心的归一化垂直距离，用来判断眉毛高低。
- `Slope`：眉毛斜率，用来判断眉形角度。

不再使用旧的 `H` / `EBHR` / `EEG` 眉毛判断，也不再使用默认男女模板兜底。

画面显示：

```text
L_BIG=当前左眉值 [合格下限,合格上限]
R_BIG=当前右眉值 [合格下限,合格上限]
B_SYM=左右BIG差值 <= 容差
S L=左眉斜率 R=右眉斜率 OK/BAD
```

当前模板在 `face_point.json` 中没有 `eyebrow` 数据时，眉毛判断不显示、不参与自动调整。先在 `ellseg_scorer.py` 里按 `B` 保存当前眉毛基线。

## BIG 计算

左眉使用 `[52, 55, 65]` 到左虹膜中心 `468` 的投影加权平均，其中靠近眉心/内眼角的 `55` 权重 `60%`，`52` 和 `65` 各 `20%`。
右眉使用 `[282, 285, 295]` 到右虹膜中心 `473` 的投影加权平均，其中靠近眉心/内眼角的 `285` 权重 `60%`，`282` 和 `295` 各 `20%`。

数值越负表示眉毛越高；数值越接近 `0` 表示眉毛越低。

## 自定义基线

按 `B` 保存后，`face_point.json` 的当前模板 `eyebrow` 保存：

```json
{
  "left_slope": 当前左眉斜率,
  "right_slope": 当前右眉斜率,
  "slope_symmetry": 左右斜率差,
  "left_brow_iris_gap": 当前左眉BIG,
  "right_brow_iris_gap": 当前右眉BIG
}
```

不同头如果需要不同标准，就分别切换或保存为不同模板后录制，数据会分别写到 `templates.<template_name>`。

Qt 面板 `qt_calibration_panel.py` 会按模板切换 `face_point.json` 中的当前分支：

```text
男女通用: common.nose / common.iris_left / common.iris_right / common.eye_line_y
模板列表: templates
当前生效: active_template
```

选择模板时，面板只更新 `active_template` 并重新加载当前模板的眼皮、眉毛、嘴部、上下唇、嘴角、头位数据。
点击各项保存按钮时，眼球基线写入 `common`；其他项目写入当前模板分支。
点击“保存当前模板”时，会把当前模板数据复制为输入的新模板名，并切换到这个模板。

自定义 BIG 范围：

```text
左眉下限 = left_brow_iris_gap - EYEBROW_CUSTOM_BIG_TOLERANCE
左眉上限 = left_brow_iris_gap + EYEBROW_CUSTOM_BIG_TOLERANCE
右眉下限 = right_brow_iris_gap - EYEBROW_CUSTOM_BIG_TOLERANCE
右眉上限 = right_brow_iris_gap + EYEBROW_CUSTOM_BIG_TOLERANCE
```

当前：

```python
EYEBROW_CUSTOM_BIG_TOLERANCE = 0.01
```

自定义 slope 范围：

```text
左眉下限 = left_slope - EYEBROW_CUSTOM_SLOPE_TOLERANCE
左眉上限 = left_slope + EYEBROW_CUSTOM_SLOPE_TOLERANCE
右眉下限 = right_slope - EYEBROW_CUSTOM_SLOPE_TOLERANCE
右眉上限 = right_slope + EYEBROW_CUSTOM_SLOPE_TOLERANCE
```

当前：

```python
EYEBROW_CUSTOM_SLOPE_TOLERANCE = 0.03
```

## 旧基线行为

首次没有 `face_point.json` 时，程序会尝试从旧文件迁移：

```text
face_points.json -> common
eyebrow_baseline_male.json -> templates.default.eyebrow
eyebrow_baseline_female.json -> templates.template_2.eyebrow
eyelid/mouth/lower_lip/upper_lip/mouth_corners/head_position 旧单份文件 -> 默认模板分支
```

旧眉毛数据如果没有 `left_brow_iris_gap/right_brow_iris_gap/left_slope/right_slope`，程序会忽略这份旧眉毛基线。需要重新按 `B` 保存一次，才会使用 BIG + slope 判断。

## 容差位置

都在 `eye_constants.py`：

```python
EYELID_EAR_TOLERANCE
EYEBROW_BIG_SYMMETRY_TOLERANCE
EYEBROW_CUSTOM_BIG_TOLERANCE
EYEBROW_CUSTOM_SLOPE_TOLERANCE
LEFT_BROW_IRIS_POINTS / RIGHT_BROW_IRIS_POINTS
LEFT_BROW_IRIS_WEIGHTS / RIGHT_BROW_IRIS_WEIGHTS
LEFT_IRIS_CENTER / RIGHT_IRIS_CENTER
```

当前策略：眉毛容错较高，不追求特别精准；必须同时满足录制基线附近的 BIG 高低范围、左右 BIG 差不过大、左右 slope 在范围内，才认为眉毛处于中性范围。
