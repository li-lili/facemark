# AGENTS.md 项目全局指令

> 每次编写、修改、调试代码前，**必须先读取并遵守**以下规则。

---

## 一、编码规范

| # | 规则 | 说明 |
|---|------|------|
| 1 | **HUD 常驻显示** | 新增参数必须在 `ellseg_scorer.py` 的 `cv2.putText` 渲染区常驻显示（不是只在 `auto_adjust_*` 调整时 `update_hud`）。单独运行 `ellseg_scorer.py` 时必须能看到所有参数。注意位置不遮挡其他信息。 |
| 2 | **参数文档同步** | 新增参数后，名称和容差值必须写入 `参数说明.md`，并告知用户容差在哪。 |
| 3 | **HUD 布局不遮挡** | 修改或新增画面文字时，必须先查看 `绘制布局.md` 和 `绘制布局可视化.html`。常驻检测参数统一放在右侧信息栏 `x=max(340,img_w-430)`，按现有顺序向下排列；自动调整过程信息只放左侧 `(10,100)/(10,118)/(10,136)` 附近。不得把新文字叠到已有参数、脸部关键区域、底部状态栏或右上 FPS 上；新增显示项必须预留行距，布局不够时先重排再添加。 |
| 4 | **眼球算法不动** | 不修改眼球检测算法。实际使用和画面输出的必须是 `dx, dy` 未处理的原始算法值。 |
| 5 | **眉毛容错高** | 眉眉调整不需要特别精准，但要保证在正常范围内，不能过高也不能过低。 |
| 6 | **不关摄像头** | 任何时候调整完成后都不要关闭摄像头。界面上显示哪些项通过，Qt 打印日志。 |

## 二、舵机运动规则

- 默认配置文件: `C:\Project\facemark\29_servo_config(13).yaml`
- 自动调整时根据此规则计算正确方向
- 首次启动从配置文件读取默认角度范围，**调整时不得超出范围**

| 舵机 | 名称 | 运动范围 | 说明 |
|------|------|----------|------|
| A0 | 左眉头 | 上(200) — 下(95) | |
| A1 | 右眉头 | 上(60) — 下(170) | 与A0机械反向 |
| A8 | 左眼皮 | 开(125) — 合(215) | 开=小角度, 合=大角度 |
| A9 | 右眼皮 | 开(145) — 合(50) | 与A8机械反向 |
| A10 | 左眼球 | 下(45) — 上(114) | |
| A11 | 右眼球 | 下(220) — 上(170) | 与A10机械反向 |
| A12 | 左眼球 | 内(190) — 外(135) | |
| A13 | 右眼球 | 内(75) — 外(135) | 与A12机械反向 |

> **注意**: A1/A9/A11/A13 与对应左侧舵机**机械方向相反**，代码中计算时需符号翻转。

### 眼球水平自动调整方向（按 raw `dX`）

- 不修改眼球检测算法，判断和日志继续使用原始 `dX, dY`。
- 眼部 `L/R` 必须按用户标记和舵机语义左右，不按摄像头画面左右：画面右侧标记为左眼 `L`，画面左侧标记为右眼 `R`；右侧 HUD 的眼球、眼皮、眉毛 L/R 也必须遵守这个定义。
- 左眼 `L dX > 0`：增加 A12。
- 左眼 `L dX < 0`：减小 A12。
- 右眼 `R dX > 0`：增加 A13。
- 右眼 `R dX < 0`：减小 A13。

---

## 三、嘴唇正侧面自动调整补充规则

> 以后修改嘴唇相关代码时必须遵守本节。正面摄像头和侧面摄像头固定，侧面摄像头默认使用 1 号摄像头，并做垂直翻转。

### 1. 上唇

| 舵机 | 名称 | 运动范围 | 说明 |
|------|------|----------|------|
| A16 | 上唇正面上下 | 上(100) — 下(190) | 根据正面 `ULR` 调整 |
| A17 | 上唇侧面前后 | 前(165) — 后(200) | 根据侧面上唇前点 `dX` 调整 |

- `保存上唇(正侧) U` 必须同时保存正面 `upper_lip.ulr` 和侧面 `upper_lip.side_upper_tip`。
- `框选侧面嘴ROI` 保存到当前模板 `upper_lip.side_roi`。
- `调整上唇(正侧)` 必须同时采样正面和侧面，A16/A17 在同一轮闭环中计算并发送。
- A17 方向：侧面上唇前点 `dX > 0` 表示太靠前，应增大角度往后调。

### 2. 下唇

| 舵机 | 名称 | 运动范围 | 说明 |
|------|------|----------|------|
| A18 | 下唇侧面前后 | 前(240) — 后(180) | 根据侧面下唇前点 `dX` 调整 |
| A19 | 下唇正面上下 | 上(210) — 下(135) | 根据正面 `LLR` 调整 |

- `保存下唇(正侧) L` 必须同时保存正面 `lower_lip.llr` 和侧面 `lower_lip.side_lower_tip`。
- `框选侧面下唇ROI` 保存到当前模板 `lower_lip.side_roi`。
- `调整下唇(正侧)` 必须同时采样正面和侧面，A19/A18 在同一轮闭环中计算并发送。
- A18 方向：侧面下唇前点 `dX > 0` 表示太靠前，应减小角度往后调。

### 3. HUD 和文档

- 新增或修改嘴唇正侧面参数时，必须同步：
  - `ellseg_scorer.py` 右侧常驻 HUD；
  - `绘制布局.md`；
  - `绘制布局可视化.html` / 对应 SVG；
  - `参数说明.md`。
- 当前嘴唇侧面容差在 `eye_constants.py`：
  - `UPPER_LIP_SIDE_X_TOLERANCE`, `UPPER_LIP_SIDE_Y_TOLERANCE`
  - `LOWER_LIP_SIDE_X_TOLERANCE`, `LOWER_LIP_SIDE_Y_TOLERANCE`
- 调整完成后不得关闭摄像头；Qt 日志必须显示保存、框选、调整和通过/失败状态。

---

## 四、已记录错误：禁止初始化自动归位

- 不得在 `initialize()`、程序启动、加载 YAML、创建 `EyeAutoTuner`、创建 `FaceController` 时自动发送舵机归位命令。
- 初始化阶段只能读取 `temp_deg`、`start_deg`、`end_deg` 等配置并更新内存状态，不能让舵机移动。
- 舵机移动必须来自明确的用户操作、手动控制按钮或自动调整流程中的真实调整步骤。
- 不能为了“解决首次移动幅度大”而加入启动归位；这会导致上次关机处于极端位置时突然大幅动作，属于已记录错误，后续不得再犯。
